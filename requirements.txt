# Core dependencies
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0

# Image/Video processing
opencv-python>=4.8.0
pillow>=10.0.0

# Optional: Mamba SSM (auto-fallback to PyTorch if unavailable)
# Requires CUDA + compilation environment
# mamba-ssm>=1.2.0

# Development (optional)
# pytest>=7.4.0
# ruff>=0.1.0
